import math
print("The Floor,square root and ceiling  of 21.56 are: " + str(math.ceil(21.56))
+ ' , '+str(math.floor(21.56)) + ' , '+ str(math.sqrt(4)))

x=10
y=-5
print("The value of x after copying the clip from y is:" + str(math.copysign(x,y)))

print("Absolute value of 96 and 56 are:", math.fabs(96)  ,  (math.fabs(56)))

print("The GCD of 24 and 56:" + str(math.gcd(24,56)))
